#include<stdio.h>
void main()
{
    int a,b,c;
    char operator;
    
    printf("Enter an operator (+, -, *, /): ");
    scanf("%c", &operator);
    
           
     printf("Enter two numbers separated by space: ");
    
    scanf("%d %d", &a , &b);

if (int)
{
    printf("Enter a Proper Operator", %d != int );
}
   


    switch (operator)
    {
        case '+': printf("Sum: %d",a+b);
                break;
        
        case '-': printf("Sub: %d", a-b);
        break;

   case '*': printf("Sub: %d", a*b);
        break;

 case '/': printf("Sub: %d", a/b);
        break;

default:
printf("Add a Proper Sign");
    }



}
